import styled from "styled-components";
import ImageBox from "./ImageBox";

function FeedRgt({ info }) {
  const Root = styled.div`
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
  `;

  return <Root>feedRgt</Root>;
}

export default FeedRgt;
